# Rest API using Python

## Before running the program, run these commands:

- `pip install Flask`
- If you don't have pip go over to the [pip installation documentation](https://pip.pypa.io/en/stable/installation/) and after you've followed the steps and downloaded it on your machine, then run `pip install Flask`
